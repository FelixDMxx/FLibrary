local functions = {
	teleportPartToPart = function(tppart,part,offset)
		tppart.CFrame = part.CFrame * offset
	end;
	getNearestPartFromPart = function(part,folder)
		print("WIP")
	end;
}

return functions
